Your goal is to update any dependencies

Do the following
    1. Run the `npm audit` to find the vulnerable installed packages in this project
    2. Run `npm audit fix` to apply updates
    3. Run tests and verify the updates didnt break anything
    